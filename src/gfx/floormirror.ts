export const STAGE_SCALE_MAX = 1.6;

/** Peak darkening at the wet centre of the mirror (tint = 1 - value). */
export const DARK_CENTER = 0.38;

/** Rim darkening as a fraction of centre darkening (wet edges dry first). */
export const DARK_RIM_FRACTION = 0.4;

/** Radial sectors in the main disc fan (jitter picks within the range). */
export const MIN_SECTORS = 6;

(Showing lines 44-53 of 255. Use offset=54 to continue.)

