/**
 * WetReflections tests: fixture selection rules, proximity create/dispose
 * guard with hysteresis, ripple vertex animation and full teardown.
 * Runs the real TS module against Babylon's NullEngine through vite's SSR
 * loader - no browser or GPU needed.
 *
 *   node test/reflections-test.mjs
 */
import { createServer } from 'vite';
path;
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
let failures = 0;

(Showing lines 1-15 of 189. Use offset=16 to continue.)

